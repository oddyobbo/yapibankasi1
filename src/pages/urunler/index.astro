---
import ProductsCatalogView from "../../components/ProductsCatalogView.astro";
import { legacyTaxonomyQueryRedirect } from "../../lib/product-taxonomy-routes.js";

const redirect = legacyTaxonomyQueryRedirect(Astro.url);
if (redirect) return Astro.redirect(redirect, 302);
---

<ProductsCatalogView />
