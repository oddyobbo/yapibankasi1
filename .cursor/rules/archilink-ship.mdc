---
description: Archilink (katalog-projesi) — canlıya alma ve kullanıcıya teslim; GitHub/Netlify UI varsayma
globs: "**/*"
alwaysApply: true
---

# Tek doğru proje (karışıklık yok)

- **Canlı site (production):** `https://cool-bienenstitch-6090eb.netlify.app/` — tüm “webde aç” URL’leri bu kökten türet (`/mimar-paneli.html`, `/urunler.html`, vb.).
- **Yerel çalışma klasörü:** `~/Desktop/katalog-projesi` — başka kopya (`antigravity`, yedek klasörler) varsayılan olarak **kullanılmaz**; kullanıcı özellikle istemedikçe oralara dokunma, oralardan deploy/push anlatma.
- **GitHub:** `https://github.com/oddyobbo/yapibankasi1` (`origin`), dal **`main`**. Netlify bu repoya bağlı; **`git push origin main`** canlı deploy’u tetikler (CLI `netlify deploy` şart değil).
- Referans dosya: kökte `canli-site-url.txt`.

# Yatay margin — header ile hizalı içerik (`layout.js` referans)

- **Kural:** Logo ile sağdaki tema / giriş–profil alanının yatay sınırları `layout.js` içindeki site header satırında: **`px-4 sm:px-6 lg:px-10`** (`h-[72px]` flex satırı). Ürün listesi (`urunler.html`), ürün detayı (`urun-unica-baffle.html`) ve benzeri tam genişlik içerik bu **aynı** yatay padding ile hizalanmalı; böylece sol kenar logo ile, sağ kenar profil butonunun bittiği yerle çakışır.
- Anasayfa (`mvp-taslak-v1.html`) bölümlerindeki daha geniş `px-8 lg:px-20` katalog sayfalarına kopyalanmamalı (header ile kayar).

# Archilink teslim standardı

- **Kullanıcı GitHub veya Netlify arayüzüne girmiyor.** “GitHub’da diff’e bak / Netlify’dan deploy et” gibi yönlendirmeler yapma; varsayılan hedef **canlı sitede güncel kod**.
- **Kod değişikliğinden sonra** (HTML/CSS/JS, `layout.js`, `core.js` vb.) mümkünse **aynı oturumda** production deploy dene:
  - Proje kökünde: `./deploy-netlify.sh` veya `netlify deploy --prod --dir=.`
- Deploy **başarısız** olursa (ör. `Forbidden`, auth):
  - Kullanıcıya **bir kez** şunu söyle: kendi Mac’inde Terminal’de proje klasöründe `netlify login` çalıştırsın; bittikten sonra agent tekrar `./deploy-netlify.sh` çalıştırabilir.
  - **Geçici önizleme** (sadece arayüz): `npx --yes serve . -l 4173` ile `http://localhost:4173` — Cursor Simple Browser veya tarayıcı; auth/Supabase yönlendirmeleri canlıdan farklı olabilir, buna kısa not düş.
- **Push:** Repo yedeklemesi için `git push` yapılabilir; kullanıcıya “GitHub’a gir” diye bir şey **önerme**.
